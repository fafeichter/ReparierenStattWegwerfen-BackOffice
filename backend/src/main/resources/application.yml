spring:
  application:
    name: Reparieren Statt Wegwerfen - Backoffice
  threads:
    virtual:
      enabled: true
  ai:
    openai:
      chat:
        model: gpt-5.5
        reasoning-effort: medium

springdoc:
  default-produces-media-type: application/json